#include <stdio.h>
int main () {
    float base, height, area;
    printf("Enter the base: ");
    scanf("%f", &base);
    printf("\nEnter the height: ");
    scanf("%f", &height);
    area = 0.5 * base * height;
    printf("\nArea of the triangle: %f", area);
    return 0;
}
