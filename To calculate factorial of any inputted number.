#include <stdio.h>
int main() {
    int i,a,p=1;
    printf("Enter any number: ");
    scanf("%d", &a);
    for(i=a;i>=1;i--)
    {
        p = p * i;
    }
    printf("\nThe factorial of %d is %d", a, p);
    return 0;
}
